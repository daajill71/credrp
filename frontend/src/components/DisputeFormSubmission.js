// DisputeFormSubmission.js
import React from 'react';

function DisputeFormSubmission({ onSubmit }) {
  return (
    <button type="button" onClick={onSubmit}>Submit</button>
  );
}

export default DisputeFormSubmission;